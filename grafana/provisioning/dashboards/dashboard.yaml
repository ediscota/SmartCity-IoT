apiVersion: 1

providers:
  - name: 'Smart City Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # IMPORTANTE: Questo è il percorso DENTRO il container, non sul tuo PC
      path: /var/lib/grafana/dashboards